<header class="zoo-header navbar navbar-expand-lg navbar-dark bg-dark shadow-lg fixed-top">
    <div class="container-fluid">
        <!-- Logo con animación y efecto hover -->
        <a href="/" class="navbar-brand animate__animated animate__fadeInLeft animate__faster d-flex align-items-center">
            <img src="/assets/images/zoo-logo.png" alt="Zoo Logo" class="logo-img me-2" style="height: 50px;">
            <span class="zoo-title d-none d-md-block fs-4 text-warning">Zoo Salvaje</span>
        </a>

        <!-- Botón hamburguesa para móviles -->
        <button class="navbar-toggler animate__animated animate__fadeInRight" 
                type="button" 
                data-bs-toggle="collapse" 
                data-bs-target="#mainNav"
                aria-label="Menú de navegación">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Menú principal con animaciones escalonadas -->
        <div class="collapse navbar-collapse justify-content-between" id="mainNav">
            <ul class="navbar-nav mx-auto mb-2 mb-lg-0">
                <li class="nav-item animate__animated animate__fadeInDown">
                    <a class="nav-link active" aria-current="page" href="/">
                        <i class="fas fa-home me-2"></i>Inicio
                    </a>
                </li>
                <li class="nav-item animate__animated animate__fadeInDown animate__delay-1s">
                    <a class="nav-link" href="/animales">
                        <i class="fas fa-paw me-2"></i>Animales
                    </a>
                </li>
                <li class="nav-item animate__animated animate__fadeInDown animate__delay-2s">
                    <a class="nav-link" href="/visitas">
                        <i class="fas fa-ticket-alt me-2"></i>Visitas
                    </a>
                </li>
                <li class="nav-item animate__animated animate__fadeInDown animate__delay-3s">
                    <a class="nav-link" href="/conservacion">
                        <i class="fas fa-leaf me-2"></i>Conservación
                    </a>
                </li>
                <li class="nav-item animate__animated animate__fadeInDown animate__delay-4s">
                    <a class="nav-link" href="/contacto">
                        <i class="fas fa-map-marked-alt me-2"></i>Contacto
                    </a>
                </li>
            </ul>

            <!-- Menú derecho con íconos -->
            <div class="d-flex align-items-center animate__animated animate__fadeInRight">
                <div class="btn-group" role="group">
                    <a href="/perfil" class="btn btn-outline-warning btn-sm mx-2" 
                       data-bs-toggle="tooltip" 
                       title="Perfil">
                        <i class="fas fa-user"></i>
                        <span class="d-none d-lg-inline ms-2">Mi Cuenta</span>
                    </a>
                    <a href="/ayuda" class="btn btn-outline-info btn-sm" 
                       data-bs-toggle="tooltip" 
                       title="Centro de ayuda">
                        <i class="fas fa-question-circle"></i>
                        <span class="d-none d-lg-inline ms-2">Ayuda</span>
                    </a>
                </div>
            </div>
        </div>
    </div>
</header>

<style>

   {# Bootstrap CSS #}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    {# Font Awesome #}
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">

    {# Animate.css #}
    <link href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" rel="stylesheet">

    {# Leaflet CSS #}
    <link href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" rel="stylesheet">

    .zoo-header {
        background: linear-gradient(45deg, #1a2f1a, #2d5a2d) !important;
        padding: 0.5rem 0;
        transition: all 0.3s ease;
    }
    
    .logo-img {
        transition: transform 0.3s ease;
    }
    
    .logo-img:hover {
        transform: rotate(-15deg) scale(1.1);
    }
    
    .nav-link {
        position: relative;
        margin: 0 1rem;
        padding: 0.5rem 1rem !important;
        border-radius: 25px;
        transition: all 0.3s ease;
    }
    
    .nav-link:hover,
    .nav-link.active {
        background: rgba(255, 255, 255, 0.1) !important;
        transform: translateY(-2px);
    }
    
    .nav-link::after {
        content: '';
        position: absolute;
        bottom: 0;
        left: 50%;
        width: 0;
        height: 2px;
        background: #ffc107;
        transition: all 0.3s ease;
    }
    
    .nav-link:hover::after,
    .nav-link.active::after {
        width: 100%;
        left: 0;
    }
</style>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>


    <script>
        // Inicializar tooltips de Bootstrap
        const tooltipTriggerList = [].slice.call(document.querySelectorAll('[data-bs-toggle="tooltip"]'))
        tooltipTriggerList.forEach(tooltipTriggerEl => {
            new bootstrap.Tooltip(tooltipTriggerEl)
        })

        // Animación al hacer scroll
        window.addEventListener('scroll', () => {
            const header = document.querySelector('.zoo-header')
            if (window.scrollY > 100) {
                header.style.transform = 'translateY(-10px)'
                header.style.opacity = '0.9'
            } else {
                header.style.transform = 'translateY(0)'
                header.style.opacity = '1'
            }
        })
    </script>
